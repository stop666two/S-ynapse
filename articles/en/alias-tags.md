---
title: Tag Alias Test
slug: alias-tags
date: 2026-08-27 09:00:00
tags: [js, cpp]
categories: [tech]
---

# Tag Alias Test

This article deliberately uses abbreviated tags (`js`, `cpp`). With `tag-aliases.json` enabled:

- `js` is normalized to `JavaScript`
- `cpp` is normalized to `C++`

The tag cloud, the article tag bars, and the tag page lists should all display the normalized names. Article card body text.
